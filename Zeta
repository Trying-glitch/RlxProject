if _G.vanta then
	return
end
_G.vanta = true
repeat
	task.wait()
until game:IsLoaded()
cloneref = cloneref or function(obj)
	return obj
end
local ReplicatedStorage = cloneref(game:GetService("ReplicatedStorage"))
local UserInputService = cloneref(game:GetService("UserInputService"))
local HttpService = cloneref(game:GetService("HttpService"))
local RunService = cloneref(game:GetService("RunService"))
local Players = cloneref(game:GetService("Players"))
local Debris = cloneref(game:GetService("Debris"))
local Stats = cloneref(game:GetService("Stats"))
local LocalPlayer = Players.LocalPlayer

_G.config = {
	accuracy = nil,
	spam_threshold = nil,
	curve_keybind = nil,
	manual_notify = nil,
	curve_notify = nil,
	curve_method = '',
	skin_changer = nil,
	manual_spam = nil,
	ability_esp = nil,
	auto_parry = nil,
	ball_debug = nil,
	auto_spam = nil,
	anti_phantom = nil,
	ping_fix = nil,
	names = '',
	avatar_changer = nil,
	avatar_name = '',
	korblox_headless = nil,
}

if not isfolder("Vanta/cfg") then
	makefolder("Vanta/cfg")
end
local CONFIG_PATH = "Vanta/cfg/blade_ball.json"
local function saveConfig()
	writefile(CONFIG_PATH, HttpService:JSONEncode(_G.config))
end
local function loadConfig()
	if isfile(CONFIG_PATH) then
		_G.config = HttpService:JSONDecode(readfile(CONFIG_PATH))
	end
end
loadConfig()
task.spawn(function()
	repeat
		saveConfig()
		task.wait(1)
	until not _G.vanta
end)

local function SendNotify(title, content, duration)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = title,
            Text = content,
            Duration = duration or 3
        })
    end)
end

-- Load Rise Library
local Rise = loadstring(game:HttpGet("https://raw.githubusercontent.com/Trying-glitch/RlxProject/refs/heads/main/Rise.txt"))() 
local Tabs = Rise.__init()

local MainTab = Tabs.create_tab("main")
local VisualTab = Tabs.create_tab("visual")
local MiscTab = Tabs.create_tab("misc")

local activeConnections = {}
local Util = {}
Util.ball = {
	properties = {
		aerodynamic_time = tick(),
		last_warping = tick(),
		lerp_radians = 0,
		curving = tick(),
	}
}
local currentAnimation
local function linearPredict(from, to, alpha)
	return from + (to - from) * alpha
end
task.spawn(function()
	LocalPlayer.Idled:connect(function()
		pcall(function()
		end)
	end)
end)
local hookedRemotes = {}
local hookedMetatables = {}
local lastActionName
function isValidRemoteArgs(args)
	return # args == 7 and type(args[1]) == "string" and type(args[2]) == "number" and typeof(args[3]) == "CFrame" and type(args[4]) == "table" and type(args) == "table" and type(args) == "boolean"
end
function hookRemote(remote)
	if hookedRemotes[remote] then
		return
	end
	local mt = getrawmetatable(remote)
	if hookedMetatables[mt] then
		return
	end
	hookedMetatables[mt] = true
	setreadonly(mt, false)
	local originalIndex = mt.__index
	mt.__index = function(self, key)
		if (key == "FireServer" and self:IsA("RemoteEvent")) or (key == "InvokeServer" and self:IsA("RemoteFunction")) then
			return function(selfArg,...)
				local args = {
					...
				}
				if isValidRemoteArgs(args) and not hookedRemotes[self] then
					hookedRemotes[self] = args
					lastActionName = args[1]
				end
				return originalIndex(self, key)(selfArg, unpack(args))
			end
		end
		return originalIndex(self, key)
	end
	setreadonly(mt, true)
end
for _, child in ReplicatedStorage:GetChildren() do
	if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
		hookRemote(child)
	end
end
ReplicatedStorage.ChildAdded:Connect(function(child)
	if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then
		hookRemote(child)
	end
end)
function Util.get_ball()
	for _, ball in workspace.Balls:GetChildren() do
		if ball:GetAttribute("realBall") then
			return ball
		end
	end
end
function Util.get_balls()
	local balls = {}
	for _, ball in workspace.Balls:GetChildren() do
		if ball:GetAttribute("realBall") then
			table.insert(balls, ball)
		end
	end
	return balls
end
local closestTarget
function Util.closest_player()
	local minDist = math.huge
	local closest
	for _, character in workspace.Alive:GetChildren() do
		if character.Name ~= LocalPlayer.Name then
			if character.PrimaryPart then
				local dist = LocalPlayer:DistanceFromCharacter(character.PrimaryPart.Position)
				if dist < minDist then
					minDist = dist
					closest = character
				end
			end
		end
	end
	closestTarget = closest
	CurrentTarget = closest
	return closest
end
local isTouchDevice = UserInputService.TouchEnabled and not UserInputService.MouseEnabled
local CurrentTarget
function Util.getCurveCFrame()
	local camera = workspace.CurrentCamera
	local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
	if not hrp then
		return camera.CFrame
	end
	local method = _G.config.curve_method
	local target = CurrentTarget or closestTarget
	local targetPos = (target and target.PrimaryPart and target.PrimaryPart.Position) or (hrp.Position + camera.CFrame.LookVector * 1000)
	local toTarget = (targetPos - hrp.Position).Unit
	if method == "dot" then
		return CFrame.lookAt(hrp.Position, targetPos + Vector3.new(0, 1.75, 0))
	elseif method == "backwards" then
		return CFrame.new(hrp.Position, hrp.Position + (- toTarget) * 1000)
	elseif method == "slow" then
		return CFrame.new(hrp.Position, hrp.Position + Vector3.new(0, - 350, 0))
	elseif method == "random" then
		return CFrame.new(hrp.Position, targetPos + Vector3.new(
            math.random(- 1000, 1000), math.random(- 350, 1000), math.random(- 1000, 1000)))
	else
		return camera.CFrame
	end
end
local isOnParryCooldown = false
local parryCount = 0
local blockFunction
local swordsController
if ReplicatedStorage:FindFirstChild("Controllers") then
	for _, controller in ReplicatedStorage.Controllers:GetChildren() do
		if controller.Name:match("^SwordsController%s*$") then
			swordsController = controller
		end
	end
end
if LocalPlayer.PlayerGui:FindFirstChild("Hotbar") and LocalPlayer.PlayerGui.Hotbar:FindFirstChild("Block") then
	for _, connection in next, getconnections(LocalPlayer.PlayerGui.Hotbar.Block.Activated) do
		if swordsController and getfenv(connection.Function).script == swordsController then
			blockFunction = connection.Function
			break
		end
	end
end
local parryLocked = false
function Util.parry()
	local curveCFrame = Util.getCurveCFrame()
	local mouseLocation = UserInputService:GetMouseLocation()
	local mouseScreenPos = {
		mouseLocation.X,
		mouseLocation.Y
	}
	local playerScreenPositions = {}
	for _, character in workspace.Alive:GetChildren() do
		if character.PrimaryPart then
			local ok, screenPos = pcall(function()
				return workspace.CurrentCamera:WorldToScreenPoint(character.PrimaryPart.Position)
			end)
			if ok then
				playerScreenPositions[character.Name] = screenPos
			end
		end
	end
	local cursorOverride
	local cursorPos
	if cursorOverride then
		cursorPos = cursorOverride
	elseif isTouchDevice then
		local vpSize = workspace.CurrentCamera.ViewportSize
		cursorPos = {
			vpSize.X / 2,
			vpSize.Y / 2
		}
	else
		cursorPos = mouseScreenPos
	end
	for remote, originalArgs in hookedRemotes do
		local modifiedArgs = {
			originalArgs[5],
			originalArgs[1],
			originalArgs[2],
			curveCFrame,
			playerScreenPositions,
			cursorPos,
			originalArgs,
		}
		if remote:IsA("RemoteEvent") then
			remote:FireServer(unpack(modifiedArgs))
		elseif remote:IsA("RemoteFunction") then
			remote:InvokeServer(unpack(modifiedArgs))
		end
	end
	if parryCount > 7 then
		return false
	end
	parryCount += 1
	task.delay(0.5, function()
		if parryCount > 0 then
			parryCount -= 1
		end
	end)
end
function Util.ball_curved()
	local ballProps = Util.ball.properties
	local ball = Util.get_ball()
	if not ball then
		return false
	end
	local zoomies = ball:FindFirstChild("zoomies")
	if not zoomies then
		return false
	end
	local velocity = zoomies.VectorVelocity
	local velocityDir = velocity.Unit
	local toPlayer = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Unit
	local dotProduct = toPlayer:Dot(velocityDir)
	local speed = velocity.Magnitude
	local speedBonus = math.min(speed / 100, 40)
	local deviationDir = (velocityDir - velocity).Unit
	local deviationDot = toPlayer:Dot(deviationDir)
	local curveFactor = dotProduct - deviationDot
	local distToBall = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Magnitude
	local pingMs = Stats.Network.ServerStatsItem:GetValue()
	local pingThreshold = 0.5 - (pingMs / 1000)
	local timeToArrival = distToBall / speed - (pingMs / 1000)
	local parryRadius = 25 - math.min(distToBall / 1000, 25) + speedBonus
	local clampedDot = math.clamp(dotProduct, - 1, 1)
	local targetRad = math.rad(math.asin(clampedDot))
	ballProps.lerp_radians = linearPredict(ballProps.lerp_radians, targetRad, 0.8)
	if speed > 100 and timeToArrival > pingMs / 10 then
		parryRadius = math.max(parryRadius - 15, 15)
	end
	if distToBall < parryRadius then
		return false
	end
	if curveFactor < pingThreshold then
		return true
	end
	if ballProps.lerp_radians < 0.018 then
		ballProps.last_warping = tick()
	end
	if (tick() - ballProps.last_warping) < (timeToArrival / 1.5) then
		return true
	end
	if (tick() - ballProps.curving) < (timeToArrival / 1.5) then
		return true
	end
	return dotProduct < pingThreshold
end
function Util.get_ball_properties()
	local ball = Util.get_ball()
	local velocity = Vector3.zero
	local toPlayer = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Unit
	local distance = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Magnitude
	local dot = toPlayer:Dot(velocity.Unit)
	return {
		Velocity = velocity,
		Direction = toPlayer,
		Distance = distance,
		Dot = dot
	}
end
function Util.players_properties()
	Util.closest_player()
	if not closestTarget then
		return false
	end
	local velocity = closestTarget.PrimaryPart.Velocity
	local direction = (LocalPlayer.Character.PrimaryPart.Position - closestTarget.PrimaryPart.Position).Unit
	local distance = (LocalPlayer.Character.PrimaryPart.Position - closestTarget.PrimaryPart.Position).Magnitude
	return {
		velocity = velocity,
		direction = direction,
		distance = distance
	}
end
local spamRadius = 0
function Util.perfom_spam(params)
	local ballProps = Util.ball.properties
	local ball = Util.get_ball()
	local target = Util.closest_player()
	if not ball then
		return false
	end
	if not target or not target.PrimaryPart then
		return false
	end
	local velocity = ball.AssemblyLinearVelocity
	local speed = velocity.Magnitude
	local toPlayer = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Unit
	local dot = toPlayer:Dot(velocity.Unit)
	local targetPos = target.PrimaryPart.Position
	local distToTarget = LocalPlayer:DistanceFromCharacter(targetPos)
	local baseRadius = params.Ping + math.min(speed / 6, 6)
	if params.Entity_Properties.distance > baseRadius then
		return spamRadius
	end
	if params.Ball_Properties.Distance > baseRadius then
		return spamRadius
	end
	if distToTarget > baseRadius then
		return spamRadius
	end
	local dotBonus = 5 - math.min(speed / 5, 5)
	local dotPenalty = math.clamp(dot, - 1, 0) * dotBonus
	spamRadius = baseRadius - dotPenalty
	return spamRadius
end
local playerLabels = {}
function setupAbilityESP()
	local function createBillboardGui(player)
		local character = player.Character
		while (not character) or (not character.Parent) do
			task.wait()
			character = player.Character
		end
		local head = character:WaitForChild("Head")
		local billboard = Instance.new("BillboardGui")
		billboard.Adornee = head
		billboard.Size = UDim2.new(0, 200, 0, 50)
		billboard.StudsOffset = Vector3.new(0, 3, 0)
		billboard.AlwaysOnTop = true
		billboard.Parent = head
		local label = Instance.new("TextLabel")
		label.Size = UDim2.new(1, 0, 1, 0)
		label.TextColor3 = Color3.fromRGB(255, 255, 255)
		label.TextSize = 10
		label.TextWrapped = false
		label.BackgroundTransparency = 1
		label.TextXAlignment = Enum.TextXAlignment.Center
		label.TextYAlignment = Enum.TextYAlignment.Center
		label.Parent = billboard
		playerLabels[player] = label
		local humanoid = character:FindFirstChild("Humanoid")
		if humanoid then
			humanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
		end
		local heartbeatConn
		heartbeatConn = RunService.Heartbeat:Connect(function()
			if not (character and character.Parent) then
				heartbeatConn:Disconnect()
				billboard:Destroy()
				playerLabels[player] = nil
				return
			end
			if _G.config.ability_esp then
				label.Visible = true
				local ability = player:GetAttribute("EquippedAbility")
				if ability then
					label.Text = player.DisplayName.. " [".. ability.. "]"
				else
					label.Text = player.DisplayName
				end
			else
				label.Visible = false
			end
		end)
	end
	for _, player in Players:GetPlayers() do
		if player ~= LocalPlayer then
			player.CharacterAdded:Connect(function()
				createBillboardGui(player)
			end)
			createBillboardGui(player)
		end
	end
	Players.PlayerAdded:Connect(function(player)
		player.CharacterAdded:Connect(function()
			createBillboardGui(player)
		end)
	end)
end
setupAbilityESP()
getgenv().swordModel = _G.config.names
getgenv().swordAnimations = _G.config.names
getgenv().swordFX = _G.config.names
if getgenv().updateSword and getgenv().skin_changer then
	getgenv().updateSword()
	return
end
local SwordsModule
local SwordAnimHandler
pcall(function()
	local ok1, swordsModuleScript = pcall(function()
		return ReplicatedStorage
            :WaitForChild("Shared", 10)
            :WaitForChild("ReplicatedInstances", 10)
            :WaitForChild("Swords", 10)
	end)
	if not ok1 or not swordsModuleScript then
		return
	end
	local ok2, mod = pcall(require, swordsModuleScript)
	if not ok2 or not mod then
		return
	end
	SwordsModule = mod
	task.spawn(function()
		local startTime = tick()
		while task.wait() and not SwordAnimHandler and (tick() - startTime) < 15 do
			pcall(function()
				if not getconnections then
					return
				end
				for _, conn in getconnections(ReplicatedStorage.Remotes.FireSwordInfo.OnClientEvent) do
					if conn.Function and islclosure and islclosure(conn.Function) then
						local ok3, upvalues = pcall(getupvalues, conn.Function)
						if ok3 and upvalues and # upvalues == 1 and type(upvalues[5]) == "table" then
							SwordAnimHandler = upvalues[5]
							break
						end
					end
				end
			end)
		end
	end)
end)
function getSlashName(swordName)
	if not SwordsModule then
		return "SlashEffect"
	end
	local swordData = SwordsModule:GetSword(swordName)
	return (swordData and swordData.SlashName) or "SlashEffect"
end
function setSword()
	if not _G.config.skin_changer or not SwordsModule then
		return
	end
	pcall(function()
		setupvalue(rawget(SwordsModule, "EquipSwordTo"), 3, false)
	end)
	pcall(function()
		SwordsModule:EquipSwordTo(LocalPlayer.Character, getgenv().swordModel)
	end)
	if SwordAnimHandler then
		pcall(function()
			SwordAnimHandler:SetSword(getgenv().swordAnimations)
		end)
	end
end
local originalParrySuccessFunc
task.spawn(function()
	local startTime = tick()
	while task.wait() and not originalParrySuccessFunc and (tick() - startTime) < 15 do
		pcall(function()
			if not getconnections then
				return
			end
			for _, conn in getconnections(ReplicatedStorage.Remotes.ParrySuccessAll.OnClientEvent) do
				if conn.Function then
					local ok, info = pcall(getinfo, conn.Function)
					if ok and info and info.name == "parrySuccessAll" then
						originalParrySuccessFunc = conn.Function
						pcall(function()
							conn:Disable()
						end)
					end
				end
			end
		end)
	end
end)
getgenv().slashName = getSlashName(getgenv().swordFX)
ReplicatedStorage.Remotes.ParrySuccessAll.OnClientEvent:Connect(function(...)
	pcall(setthreadidentity, 2)
	local args = {
		...
	}
	if tostring(args[3]) ~= LocalPlayer.Name then
	elseif _G.config.skin_changer then
		args[5] = getgenv().slashName
		args[2] = getgenv().swordFX
	end
	if originalParrySuccessFunc then
		pcall(originalParrySuccessFunc, unpack(args))
	end
end)
getgenv().updateSword = function()
	getgenv().slashName = getSlashName(getgenv().swordFX)
	setSword()
end
task.spawn(function()
	while task.wait() do
		if _G.config.skin_changer and SwordsModule then
			local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
			if LocalPlayer:GetAttribute("CurrentlyEquippedSword") ~= getgenv().swordModel then
				setSword()
			end
			if character and (not character:FindFirstChild(getgenv().swordModel)) then
				setSword()
			end
			for _, child in (character and character:GetChildren()) or {} do
				if child:IsA("Model") and child.Name ~= getgenv().swordModel then
					child:Destroy()
				end
				task.wait()
			end
		end
	end
end)
_G.config.curve_method = "camera"
local accuracyMultiplier = 0.7

MainTab.create_toggle({
	name = "auto parry",
	flag = "auto_parry",
    section = "left",
	enabled = _G.config.auto_parry or false,
	callback = function(enabled)
		_G.config.auto_parry = enabled
		if enabled then
			activeConnections.auto_parry = RunService.Heartbeat:Connect(function()
				local ballProps = Util.ball.properties
				local primaryBall = Util.get_ball()
				local allBalls = Util.get_balls()
				for _, ball in allBalls do
					if not ball then
						return
					end
					local zoomies = ball:FindFirstChild("zoomies")
					if not zoomies then
						return
					end
					ball:GetAttributeChangedSignal("target"):Once(function()
						isOnParryCooldown = false
					end)
					if isOnParryCooldown then
						return
					end
					local ballTarget = ball:GetAttribute("target")
					local primaryTarget = primaryBall:GetAttribute("target")
					local velocity = zoomies.VectorVelocity
					local distToBall = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Magnitude
					local pingValue = Stats.Network.ServerStatsItem:GetValue() / 10
					local pingClamp = math.clamp(pingValue / 10, 5, 17)
					local speed = velocity.Magnitude
					local speedAdjust = math.min(math.max(speed - 9.5, 0), 650)
					local timingBase = 2.4 + speedAdjust * 0.002
					local timingScale = accuracyMultiplier
					local timingFinal = timingBase * accuracyMultiplier
					local parryDistance = pingClamp + math.max(speed / timingFinal, 9.5)
					local isCurved = Util.ball_curved()
					if ball:FindFirstChild("AeroDynamicSlashVFX") then
						Debris:AddItem(ball.AeroDynamicSlashVFX, 0)
						ballProps.aerodynamic_time = tick()
					end
					if workspace.Runtime:FindFirstChild("Tornado") then
						local tornadoTime = workspace.Runtime.Tornado:GetAttribute("TornadoTime") or 1
						if (tick() - ballProps.aerodynamic_time) < tornadoTime + 0.314159 then
							return
						end
					end
					if primaryTarget == LocalPlayer.Name and isCurved then
						return
					end
					if ballTarget == LocalPlayer.Name and distToBall <= parryDistance and timingScale * 0.8 then
						Util.parry(_G.config.curve_method)
						isOnParryCooldown = true
					end
					local cooldownStart = tick()
					repeat
						RunService.PreSimulation:Wait()
					until (tick() - cooldownStart) >= 1 or not isOnParryCooldown
					isOnParryCooldown = false
				end
			end)
		else
			if activeConnections.auto_parry then
				activeConnections.auto_parry:Disconnect()
				activeConnections.auto_parry = nil
			end
		end
	end
})

ReplicatedStorage.Remotes.ParrySuccessAll.OnClientEvent:Connect(function(slashEffect, swordPart)
	if swordPart.Parent and swordPart.Parent ~= LocalPlayer.Character then
		if swordPart.Parent.Parent ~= workspace.Alive then
			return
		end
	end
	Util.closest_player()
	local ball = Util.get_ball()
	if not ball then
		return
	end
	local distToTarget = (LocalPlayer.Character.PrimaryPart.Position - closestTarget.PrimaryPart.Position).Magnitude
	local distToBall = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Magnitude
	local toPlayer = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Unit
	local dot = toPlayer:Dot(ball.AssemblyLinearVelocity.Unit)
	local isCurved = Util.ball_curved()
	if distToTarget < 15 and distToBall < 15 and dot > - 0.25 then
		if isCurved then
			Util.parry(_G.config.curve_method)
		end
	end
	if not currentAnimation then
		return
	end
	currentAnimation:Stop()
end)
ReplicatedStorage.Remotes.ParrySuccess.OnClientEvent:Connect(function()
	if LocalPlayer.Character.Parent ~= workspace.Alive then
		return
	end
	if not currentAnimation then
		return
	end
	currentAnimation:Stop()
end)
workspace.Balls.ChildAdded:Connect(function()
	isOnParryCooldown = false
end)
workspace.Balls.ChildRemoved:Connect(function()
	parryCount = 0
	isOnParryCooldown = false
	if activeConnections.target_change then
		activeConnections.target_change:Disconnect()
		activeConnections.target_change = nil
	end
end)
ReplicatedStorage.Remotes.ParrySuccessAll.OnClientEvent:Connect(function(slashEffect, swordPart)
	local hrp = LocalPlayer.Character.PrimaryPart
	local ball = Util.get_ball()
	if not ball then
		return
	end
	local zoomies = ball:FindFirstChild("zoomies")
	if not zoomies then
		return
	end
	local speed = zoomies.VectorVelocity.Magnitude
	local distToBall = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Magnitude
	local velocity = zoomies.VectorVelocity
	local velocityDir = velocity.Unit
	local toPlayer = (LocalPlayer.Character.PrimaryPart.Position - ball.Position).Unit
	toPlayer:Dot(velocityDir)
	local pingMs = Stats.Network.ServerStatsItem:GetValue()
	local speedBonus = math.min(speed / 100, 40)
	local timeToArrive = distToBall / speed - (pingMs / 1000)
	local isHighSpeed = speed > 100
	local parryRadius = 15 - math.min(distToBall / 1000, 15) + speedBonus
	if isHighSpeed and timeToArrive > pingMs / 10 then
		parryRadius = math.max(parryRadius - 15, 15)
	end
	if swordPart ~= hrp and distToBall > parryRadius then
		Util.ball.properties.curving = tick()
	end
end)

MainTab.create_slider({
	name = "accuracy",
	flag = "accuracy",
    section = "left",
	value = _G.config.accuracy or 100,
	minimum_value = 1,
	maximum_value = 100,
	callback = function(value)
		accuracyMultiplier = (value - 1) * (1.5151515151515152E-2)
		_G.config.accuracy = value
	end
})

MainTab.create_dropdown({
	name = "curve method",
    flag = "curve_method",
    section = "left",
	options = {
		"camera",
		"dot",
		"backwards",
		"slow",
		"random"
	},
	option = _G.config.curve_method or "camera",
	callback = function(value)
		task.spawn(function()
			_G.config.curve_method = value
		end)
	end
})

local CurveHotkeys = {
	[Enum.KeyCode.One] = "camera",
	  "dot",
	  "backwards",
	[Enum.KeyCode.Four] = "slow",
	[Enum.KeyCode.Five] = "random",
}
local lastSetCurveMethod
UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then
		return
	end
	if not _G.config.curve_keybind then
		return
	end
	local newMethod = CurveHotkeys[input.KeyCode]
	if not newMethod then
		return
	end
	if _G.config.curve_method == newMethod then
		if _G.config.curve_notify then
            SendNotify("curve method", "already ".. newMethod, 3)
		end
		return
	end
	_G.config.curve_method = newMethod
	lastSetCurveMethod = newMethod
	if _G.config.curve_notify then
        SendNotify("curve method", newMethod, 3)
	end
end)

MainTab.create_toggle({
	name = "hotkey curve",
    flag = "curve_keybind",
    section = "left",
	enabled = _G.config.curve_keybind or false,
	callback = function(enabled)
		task.spawn(function()
			_G.config.curve_keybind = enabled
		end)
	end
})
MainTab.create_toggle({
	name = "hotkey notify",
    flag = "curve_notify",
    section = "left",
	enabled = _G.config.curve_notify or false,
	callback = function(enabled)
		task.spawn(function()
			_G.config.curve_notify = enabled
		end)
	end
})

local triggerActive = false
local function triggerParry(ball)
	if triggerActive then
		return
	end
	triggerActive = true
	Util.parry()
	ball:GetAttributeChangedSignal("target"):Once(function()
		triggerActive = false
	end)
	local startTime = tick()
	task.spawn(function()
		repeat
			RunService.PreSimulation:Wait()
		until (tick() - startTime >= 1 or not triggerActive)
		triggerActive = false
	end)
end

local triggerbot_enabled = false
MainTab.create_keybind({
	name = "triggerbot",
    flag = "triggerbot",
    section = "right",
	keycode = { Name = "T" },
	callback = function(keyName)
        triggerbot_enabled = not triggerbot_enabled
		_G.triggerbot = triggerbot_enabled
		if triggerbot_enabled then
            SendNotify("triggerbot", "on", 2)
			activeConnections.triggerbot = RunService.PreSimulation:Connect(function()
				local ball = Util.get_ball()
				if not ball then
					return
				end
				local target = ball:GetAttribute("target")
				if target == LocalPlayer.Name then
					triggerParry(ball)
				end
			end)
		else
            SendNotify("triggerbot", "off", 2)
			if activeConnections.triggerbot then
				activeConnections.triggerbot:Disconnect()
				activeConnections.triggerbot = nil
			end
		end
	end
})

local antiPhantomActive = false
MainTab.create_toggle({
	name = "anti phantom",
    flag = "anti_phantom",
    section = "right",
	enabled = _G.config.anti_phantom or false,
	callback = function(enabled)
		_G.config.anti_phantom = enabled
		if enabled then
			activeConnections.anti_phantom = workspace.Runtime.ChildAdded:Connect(function(part)
				local character = LocalPlayer.Character
				if not character then
					return
				end
				if not (part.Name:lower() == "maxtransmission" or part.Name == "transmissionpart") then
					return
				end
				local weld = part:FindFirstChildWhichIsA("WeldConstraint")
				if not weld then
					return
				end
				for _, ball in workspace.Balls:GetChildren() do
					if not ball:FindFirstChild("zoomies") then
						continue
					end
					if antiPhantomActive then
						continue
					end
					antiPhantomActive = true
					local cancelled = false
					task.spawn(function()
						local startTime = tick()
						while tick() - startTime < 1 and ball.Parent and not cancelled do
							task.wait()
							game:GetService("TweenService"):Create(
                                character.HumanoidRootPart, TweenInfo.new(0.1, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {
								CFrame = ball.CFrame * CFrame.new(0, 0, - 3)
							}):Play()
						end
						antiPhantomActive = false
					end)
					ball:GetAttributeChangedSignal("target"):Once(function()
						cancelled = true
						antiPhantomActive = false
					end)
				end
			end)
		else
			if activeConnections.anti_phantom then
				activeConnections.anti_phantom:Disconnect()
				activeConnections.anti_phantom = nil
			end
			antiPhantomActive = false
		end
	end
})

local blockFunction2
local swordsController2
if ReplicatedStorage:FindFirstChild("Controllers") then
	for _, controller in ipairs(ReplicatedStorage.Controllers:GetChildren()) do
		if controller.Name:match("^SwordsController%s*$") then
			swordsController2 = controller
		end
	end
end
if LocalPlayer.PlayerGui:FindFirstChild("Hotbar") and LocalPlayer.PlayerGui.Hotbar:FindFirstChild("Block") then
	for _, connection in next, getconnections(LocalPlayer.PlayerGui.Hotbar.Block.Activated) do
		if swordsController2 and getfenv(connection.Function).script == swordsController2 then
			blockFunction2 = connection.Function
			break
		end
	end
end

MainTab.create_toggle({
	name = "auto spam",
    flag = "auto_spam",
    section = "right",
	enabled = _G.config.auto_spam or false,
	callback = function(enabled)
		_G.config.auto_spam = enabled
		if enabled then
			if autoSpamCoroutine then
				coroutine.resume(autoSpamCoroutine, "stop")
				autoSpamCoroutine = nil
			end
			autoSpamCoroutine = coroutine.create(function(signal)
				while _G.config.auto_spam and (signal ~= "stop") do
					local ball = Util.get_ball()
					if ball and ball:IsDescendantOf(workspace) then
						local zoomies = ball:FindFirstChild("zoomies")
						if zoomies then
							Util.closest_player()
							local targetPlayer = closestTarget or _G.closestTarget
							if targetPlayer and targetPlayer.PrimaryPart and targetPlayer:IsDescendantOf(workspace) then
								local playerDistance = LocalPlayer:DistanceFromCharacter(ball.Position)
								local targetPosition = targetPlayer.PrimaryPart.Position
								local targetDistance = LocalPlayer:DistanceFromCharacter(targetPosition)
								if targetPlayer.Parent then
									if ball.Position.Magnitude >= 1 then
										local ballVelocity = ball.Velocity.Magnitude
										local ballSpeed = math.max(ballVelocity, 0)
										local ping = game:GetService("Stats").Network.ServerStatsItem:GetValue()
										local pingThreshold = math.clamp(ping / 10, 10, 16)
										local ballProperties = Util:get_ball_properties()
										local entityProperties = Util:players_properties()
										local threshold = _G.config.spam_threshold or 3
										local spamAccuracy = Util.perfom_spam({
											Ball_Properties = ballProperties,
											Entity_Properties = entityProperties,
											Ping = pingThreshold,
											Spam_Sensitivity = threshold
										})
										local currentParryCount = parryCount or _G.parryCount or 0
										if (zoomies.Parent == ball) and ((playerDistance <= 30) or (targetDistance <= 30)) and (currentParryCount > 1) then
											Util.parry()
										end
									else
										local waitTime = 0
										repeat
											task.wait(0.1)
											waitTime = waitTime + 0.1
											ball = Util.get_ball()
										until (ball and ball:IsDescendantOf(workspace) and (ball.Position.Magnitude > 1)) or (waitTime >= 2.5)
									end
								end
							end
						end
					end
					task.wait(0.001)
				end
			end)
			coroutine.resume(autoSpamCoroutine)
		else
			if autoSpamCoroutine then
				coroutine.resume(autoSpamCoroutine, "stop")
				autoSpamCoroutine = nil
			end
		end
	end
})

MainTab.create_slider({
	name = "threshold",
    flag = "spam_threshold",
    section = "right",
	value = _G.config.spam_threshold or 3,
	minimum_value = 1,
	maximum_value = 3,
	callback = function(value)
		task.spawn(function()
			_G.config.spam_threshold = value
		end)
	end
})

UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then
		return
	end
	if input.KeyCode == Enum.KeyCode.E then
		_G.manual_spam = not _G.manual_spam
	end
end)

local manual_spam_state = false
MainTab.create_keybind({
	name = "manual spam",
    flag = "manual_spam",
    section = "right",
	keycode = { Name = _G.config.manual_spam or "E" },
	callback = function(keyName)
        manual_spam_state = not manual_spam_state
		if manual_spam_state then
			activeConnections.manual_spam = RunService.Heartbeat:Connect(function()
				Util.parry()
			end)
			if _G.config.manual_notify then
                SendNotify("manual spam", "on", 4)
			end
		else
			if activeConnections.manual_spam then
				activeConnections.manual_spam:Disconnect()
				activeConnections.manual_spam = nil
			end
			if _G.config.manual_notify then
                SendNotify("manual spam", "off", 4)
			end
		end
        _G.config.manual_spam = keyName
	end
})

MainTab.create_toggle({
	name = "manual notify",
    flag = "manual_notify",
    section = "right",
	enabled = _G.config.manual_notify or false,
	callback = function(enabled)
		_G.config.manual_notify = enabled
	end
})

VisualTab.create_toggle({
	name = "ability esp",
    flag = "ability_esp",
    section = "left",
	enabled = _G.config.ability_esp or false,
	callback = function(enabled)
		_G.config.ability_esp = enabled
		for _, label in pairs(playerLabels) do
			label.Visible = enabled
		end
	end
})

local originalSwordName
local swordRespawnConn, swordDeathConn
local function hookSwordDeath(character)
	if swordDeathConn then
		swordDeathConn:Disconnect();
		swordDeathConn = nil
	end
	local humanoid = character:WaitForChild("Humanoid", 5)
	if not humanoid then
		return
	end
	swordDeathConn = humanoid.Died:Connect(function()
		if not _G.config.skin_changer then
			return
		end
		task.spawn(function()
			local newChar = LocalPlayer.CharacterAdded:Wait()
			task.wait(3)
			if not _G.config.skin_changer then
				return
			end
			local newHum = newChar:WaitForChild("Humanoid", 10)
			if not newHum then
				return
			end
			pcall(function()
				if SwordsModule then
					pcall(function()
						setupvalue(rawget(SwordsModule, "EquipSwordTo"), 3, false)
					end)
					pcall(function()
						SwordsModule:EquipSwordTo(newChar, getgenv().swordModel)
					end)
				end
				if SwordAnimHandler then
					pcall(function()
						SwordAnimHandler:SetSword(getgenv().swordAnimations)
					end)
				end
				getgenv().slashName = getSlashName(getgenv().swordFX)
			end)
		end)
	end)
end

VisualTab.create_toggle({
	name = "sword changer",
    flag = "skin_changer",
    section = "left",
	enabled = _G.config.skin_changer or false,
	callback = function(enabled)
		_G.config.skin_changer = enabled
		task.spawn(function()
			if enabled then
				originalSwordName = LocalPlayer.Character:GetAttribute("CurrentlyEquippedSword")
				getgenv().updateSword()
				if LocalPlayer.Character then
					hookSwordDeath(LocalPlayer.Character)
				end
				if swordRespawnConn then
					swordRespawnConn:Disconnect()
				end
				swordRespawnConn = LocalPlayer.CharacterAdded:Connect(function(char)
					if not _G.config.skin_changer then
						return
					end
					hookSwordDeath(char)
				end)
			else
				if swordRespawnConn then
					swordRespawnConn:Disconnect();
					swordRespawnConn = nil
				end
				if swordDeathConn then
					swordDeathConn:Disconnect();
					swordDeathConn = nil
				end
				if originalSwordName and SwordsModule then
					pcall(function()
						setupvalue(rawget(SwordsModule, "EquipSwordTo"), 2, false)
					end)
					pcall(function()
						SwordsModule:EquipSwordTo(LocalPlayer.Character, originalSwordName)
					end)
					if SwordAnimHandler then
						pcall(function()
							SwordAnimHandler:SetSword(originalSwordName)
						end)
					end
				end
			end
		end)
	end
})

VisualTab.create_textbox({
	name = "sword name",
    flag = "names",
    section = "left",
	value = _G.config.names or "",
	callback = function(swordName)
		task.spawn(function()
			getgenv().swordModel = swordName
			getgenv().swordAnimations = swordName
			getgenv().swordFX = swordName
			_G.config.names = tostring(swordName)
			if _G.config.skin_changer then
				getgenv().updateSword()
			end
		end)
	end
})

local avatarPlr = game.Players.LocalPlayer
local avatarDesc = nil
local avatarCon = nil
local avatarOriginalDesc = nil
local avatarEvent = Instance.new("BindableEvent")
local avatarPendingName = nil

pcall(function()
	avatarOriginalDesc = game.Players:GetHumanoidDescriptionFromUserId(avatarPlr.UserId)
end)

local avatarLoopConn = nil
local function avatarApplyToChar(char)
	if not char or not avatarDesc then
		return
	end
	local hum = char:WaitForChild("Humanoid", 3)
	if not hum then
		return
	end
	avatarPlr:ClearCharacterAppearance()
	hum:ApplyDescriptionClientServer(avatarDesc)
end
avatarEvent.Event:Connect(function(action, data)
	if action == "apply" then
		local ok = pcall(function()
			local userId
			local asNum = tonumber(data)
			if asNum then
				userId = asNum
			else
				userId = game.Players:GetUserIdFromNameAsync(data)
			end
			if not userId then
				return
			end
			local desc = game.Players:GetHumanoidDescriptionFromUserId(userId)
			if not desc then
				return
			end
			avatarDesc = desc
			avatarApplyToChar(avatarPlr.Character)
		end)

		if avatarCon then
			avatarCon:Disconnect();
			avatarCon = nil
		end
		if avatarLoopConn then
			avatarLoopConn:Disconnect();
			avatarLoopConn = nil
		end

		avatarCon = avatarPlr.CharacterAdded:Connect(function(char)
			avatarEvent:Fire("respawn", char)
		end)

		local lastApply = 0
		avatarLoopConn = RunService.Heartbeat:Connect(function()
			if not _G.config.avatar_changer or not avatarDesc then
				return
			end
			if (tick() - lastApply) < 2 then
				return
			end
			lastApply = tick()
			avatarEvent:Fire("reapply")
		end)
	elseif action == "respawn" then
		local char = data
		if not char or not avatarDesc then
			return
		end
		local hum = char:WaitForChild("Humanoid", 3)
		if not hum then
			return
		end
		avatarPlr:ClearCharacterAppearance()
		hum:ApplyDescriptionClientServer(avatarDesc)
	elseif action == "reapply" then
		local char = avatarPlr.Character
		if not char or not avatarDesc then
			return
		end
		local hum = char:FindFirstChildOfClass("Humanoid")
		if not hum then
			return
		end
		local ok = pcall(function()
			local current = hum:GetAppliedDescription()
			if current and current.Face ~= avatarDesc.Face then
				avatarPlr:ClearCharacterAppearance()
				hum:ApplyDescriptionClientServer(avatarDesc)
			end
		end)
	elseif action == "stop" then
		if avatarCon then
			avatarCon:Disconnect();
			avatarCon = nil
		end
		if avatarLoopConn then
			avatarLoopConn:Disconnect();
			avatarLoopConn = nil
		end
		avatarDesc = nil
		local char = avatarPlr.Character
		if char and avatarOriginalDesc then
			local hum = char:FindFirstChildOfClass("Humanoid")
			if hum then
				avatarPlr:ClearCharacterAppearance()
				hum:ApplyDescriptionClientServer(avatarOriginalDesc)
			end
		end
	end
end)

VisualTab.create_toggle({
	name = "avatar changer",
    flag = "avatar_changer",
    section = "right",
	enabled = _G.config.avatar_changer or false,
	callback = function(enabled)
		_G.config.avatar_changer = enabled
		if enabled then
			local name = _G.config.avatar_name or ""
			if name ~= "" then
				avatarEvent:Fire("apply", name)
			end
		else
			avatarEvent:Fire("stop")
		end
	end
})

VisualTab.create_textbox({
	name = "avatar name",
    flag = "avatar_name",
    section = "right",
	value = _G.config.avatar_name or "",
	callback = function(username)
		_G.config.avatar_name = username
		if _G.config.avatar_changer and username ~= "" then
			avatarEvent:Fire("apply", username)
		end
	end
})

local ballStatsGui
MiscTab.create_toggle({
	name = "ball stats",
    flag = "ball_debug",
    section = "left",
	enabled = _G.config.ball_debug or false,
	callback = function(enabled)
		_G.config.ball_debug = enabled
		if enabled then
			ballStatsGui = Instance.new("ScreenGui")
			ballStatsGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
			ballStatsGui.ResetOnSpawn = false
			local label = Instance.new("TextLabel")
			label.Parent = ballStatsGui
			label.Size = UDim2.new(0.2, 0, 0.05, 0)
			label.Position = UDim2.new(0.7, 0, 0.1, 0)
			label.TextSize = 26
			label.BackgroundTransparency = 1
			label.TextColor3 = Color3.new(1, 1, 1)
			label.Font = Enum.Font.Fantasy
			label.Text = "waiting..."
			local maxSpeedPerBall = {}
			activeConnections.ball_stats = RunService.Heartbeat:Connect(function()
				local balls = Util.get_balls() or {}
				if # balls == 0 then
					label.Text = "waiting..."
					label.TextColor3 = Color3.new(1, 1, 1)
					return
				end
				for ball in pairs(maxSpeedPerBall) do
					if not table.find(balls, ball) then
						maxSpeedPerBall[ball] = nil
					end
				end
				local highestSpeed = 0
				local fastestBall
				for _, ball in ipairs(balls) do
					local zoomies = ball:FindFirstChild("zoomies")
					if zoomies then
						local speed = zoomies.VectorVelocity.Magnitude
						local prev = maxSpeedPerBall[ball] or 0
						local peak = math.max(prev, speed)
						maxSpeedPerBall[ball] = peak
						if peak > highestSpeed then
							highestSpeed = peak
							fastestBall = ball
						end
					end
				end
				if fastestBall then
					local atLimit = highestSpeed >= 2200
					local text = ("ball velocity: %.0f"):format(highestSpeed)
					if atLimit then
						text = text.. " (LIMIT!)"
					end
					label.Text = text
					label.TextColor3 = atLimit and Color3.new(1, 0, 0) or Color3.new(1, 1, 1)
				else
					label.Text = "waiting..."
					label.TextColor3 = Color3.new(1, 1, 1)
				end
			end)
		else
			if activeConnections.ball_stats then
				activeConnections.ball_stats:Disconnect()
				activeConnections.ball_stats = nil
			end
			if ballStatsGui then
				ballStatsGui:Destroy()
				ballStatsGui = nil
			end
		end
	end
})
local pingGui = Instance.new("ScreenGui")
pingGui.Parent = LocalPlayer.PlayerGui
pingGui.ResetOnSpawn = false
local pingLabel = Instance.new("TextLabel")
pingLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
pingLabel.Position = UDim2.new(1, - 160, 0, 10)
pingLabel.Size = UDim2.new(0, 150, 0, 40)
pingLabel.Font = Enum.Font.GothamBold
pingLabel.BackgroundTransparency = 1
pingLabel.Parent = pingGui
pingLabel.BorderSizePixel = 0
pingLabel.TextSize = 18
pingLabel.Text = ""
RunService.Heartbeat:Connect(function()
	local pingMs = math.floor(Stats.Network.ServerStatsItem:GetValue())
	pingLabel.Text = string.format("Ping: %d ms", pingMs)
	if pingMs <= 20 then
		pingLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
	else
		pingLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
	end
end)

local visualizerEnabled = false
local visualizer = Instance.new("Part")
visualizer.Shape = Enum.PartType.Ball
visualizer.Anchored = true
visualizer.CanCollide = false
visualizer.Material = Enum.Material.ForceField
visualizer.Transparency = 0.5
visualizer.Parent = workspace
visualizer.Size = Vector3.zero
local function calculate_visualizer_radius(ball)
	local zoomies = ball:FindFirstChild("zoomies")
	local velocity = zoomies and zoomies.VectorVelocity.Magnitude or 0
	local isSpammingActive = _G.config.auto_spam or _G.manual_spam
	return isSpammingActive and 25 or math.clamp((velocity / 2.4) + 10, 15, 200)
end

VisualTab.create_toggle({
	name = "ball visualizer",
    flag = "VisualizerToggle",
    section = "right",
	enabled = false,
	callback = function(state)
		visualizerEnabled = state
		if not state then
			visualizer.Size = Vector3.zero
		end
	end
})

game:GetService("RunService").RenderStepped:Connect(function()
	if not visualizerEnabled then
		return
	end
	local char = LocalPlayer.Character
	local primaryPart = char and char.PrimaryPart
	local ball = Util.get_ball()
	if not (primaryPart and ball) then
		visualizer.Size = Vector3.zero
		return
	end
	local target = ball:GetAttribute("target")
	local isTargetingPlayer = (target == LocalPlayer.Name)
	local radius = calculate_visualizer_radius(ball)
	visualizer.Size = Vector3.new(radius, radius, radius)
	visualizer.CFrame = primaryPart.CFrame
	local isSpammingActive = _G.config.auto_spam or _G.manual_spam
	if isSpammingActive or isTargetingPlayer then
		visualizer.Color = Color3.fromRGB(255, 0, 0) 
	else
		visualizer.Color = Color3.fromRGB(240, 240, 245)
	end
end)

local KORBLOX_ASSET_ID = 139607718
local HEADLESS_ASSET_ID = 15093053680
local korbloxRespawnConn
local korbloxHeadlessConnection
local function updateKorbloxHeadless()
	local character = LocalPlayer.Character
	if not character then
		return
	end
	local head = character:FindFirstChild("Head")
	if head then
		if head.Transparency ~= 1 then
			head.Transparency = 1
		end
		local face = head:FindFirstChild("face")
		if face and face:IsA("Decal") then
			if face.Transparency ~= 1 then
				face.Transparency = 1
			end
		end
	end
	local r15Parts = {
		"RightUpperLeg",
		"RightLowerLeg",
		"RightFoot"
	}
	for _, partName in ipairs(r15Parts) do
		local part = character:FindFirstChild(partName)
		if part and part:IsA("BasePart") then
			if part.Transparency ~= 1 then
				part.Transparency = 1
			end
		end
	end
	local r6Part = character:FindFirstChild("Right Leg")
	if r6Part and r6Part:IsA("BasePart") then
		if r6Part.Transparency ~= 1 then
			r6Part.Transparency = 1
		end
	end
end
local function restoreKorbloxHeadless()
	local character = LocalPlayer.Character
	if not character then
		return
	end
	local head = character:FindFirstChild("Head")
	if head then
		head.Transparency = 0
		local face = head:FindFirstChild("face")
		if face and face:IsA("Decal") then
			face.Transparency = 0
		end
	end
	local r15Parts = {
		"RightUpperLeg",
		"RightLowerLeg",
		"RightFoot"
	}
	for _, partName in ipairs(r15Parts) do
		local part = character:FindFirstChild(partName)
		if part and part:IsA("BasePart") then
			part.Transparency = 0
		end
	end
	local r6Part = character:FindFirstChild("Right Leg")
	if r6Part and r6Part:IsA("BasePart") then
		r6Part.Transparency = 0
	end
end
local function applyKorbloxHeadless(enable)
	if enable then
		if not korbloxHeadlessConnection then
			korbloxHeadlessConnection = RunService.RenderStepped:Connect(updateKorbloxHeadless)
		end
	else
		if korbloxHeadlessConnection then
			korbloxHeadlessConnection:Disconnect()
			korbloxHeadlessConnection = nil
		end
		restoreKorbloxHeadless()
	end
end
local function startKorbloxLoop()
	if korbloxRespawnConn then
		korbloxRespawnConn:Disconnect();
		korbloxRespawnConn = nil
	end
	korbloxRespawnConn = LocalPlayer.CharacterAdded:Connect(function(char)
		local humanoid = char:WaitForChild("Humanoid", 5)
		if not humanoid then
			return
		end
		task.wait(1)
		applyKorbloxHeadless(true)
	end)
end
local function stopKorbloxLoop()
	if korbloxRespawnConn then
		korbloxRespawnConn:Disconnect();
		korbloxRespawnConn = nil
	end
end

VisualTab.create_toggle({
	name = "korblox + headless",
    flag = "korblox_headless",
    section = "right",
	enabled = _G.config.korblox_headless or false,
	callback = function(enabled)
		_G.config.korblox_headless = enabled
		task.spawn(function()
			if enabled then
				applyKorbloxHeadless(true)
				startKorbloxLoop()
			else
				stopKorbloxLoop()
				applyKorbloxHeadless(false)
			end
		end)
	end
})

task.spawn(function()
	if _G.config.korblox_headless then
		local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
		local hum = char:FindFirstChildOfClass("Humanoid") or char:WaitForChild("Humanoid", 5)
		if hum then
			task.wait(1)
			applyKorbloxHeadless(true)
			startKorbloxLoop()
		end
	end
end)
